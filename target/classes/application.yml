server:
  port: 8080

spring:
  application:
    name: milktea-app
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8

# 百度AI配置（免费额度）
baidu:
  ai:
    app-id: 7537111
    api-key: 6r6KDUQ07P7WmLMsFLjqjiWN
    secret-key: qSOQbCOWbpN2jAG0xQ0630YiW65MqkmT
    # 图像识别接口
    image-recognition-url: https://aip.baidubce.com/rest/2.0/image-classify/v2/advanced_general

# Deepseek AI配置（用于AI辅助推荐）
deepseek:
  api:
    key: sk-5d491f4f90a24091ae43de5e2a18f081
    url: https://api.deepseek.com/v1/chat/completions

# 文件上传路径
file:
  upload-path: ./uploads/

# 图片预处理配置
image:
  preprocessor:
    enabled: true
    python:
      path: python
    script:
      path: scripts/image_preprocessor.py
    timeout: 30

# 图片预处理配置
image:
  preprocessor:
    enabled: true
    python:
      path: python
    script:
      path: scripts/image_preprocessor.py
    timeout: 30
